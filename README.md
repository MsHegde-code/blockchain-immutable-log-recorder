## To run the project
1. Place the log files (.evtx, .log, .txt) in the directory "/backend/data/logs"
2. Start the backend server by executing "python app.py" command in the terminal (/backend path)
3. Start the development frontend server by executing "npm run dev" command in the terminal (frontend/blockchainViewer path)
